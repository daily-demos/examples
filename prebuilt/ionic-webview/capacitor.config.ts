import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app.Ionic-webview',
  appName: 'ionic-webview',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
